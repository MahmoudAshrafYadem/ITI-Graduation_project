# LTE KPI Degradation Analyzer - Dependencies
# Install with: pip install -r requirements.txt

# Core analysis dependencies
numpy>=1.20.0
pandas>=1.3.0
scipy>=1.7.0

# Plotting (Word report figures / desktop GUI)
matplotlib>=3.4.0

# Word document generation
python-docx>=0.8.11

# Excel file support
#   openpyxl reads/writes the .xlsx workbooks used by this project.
#   NOTE: xlrd was intentionally removed — xlrd>=2.0 dropped .xlsx support and
#   only reads legacy .xls, so it cannot open "New Cairo KPIs.xlsx". openpyxl
#   is the correct and sufficient engine for the .xlsx data here.
openpyxl>=3.0.0

# Streamlit dashboard (app_streamlit.py)
streamlit>=1.20.0

# Forecasting module (forcasting_section/xg_forecast_app.py)
plotly>=5.0.0
scikit-learn>=1.0.0
xgboost>=1.5.0
statsmodels>=0.13.0
