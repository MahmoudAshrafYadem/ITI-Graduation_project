% ============================================================
%  SECTION 3 — Khaled Mogahed
%  Optimization Action Analyzer
%
%  SCREENSHOTS → drop files into:  screenshots/khaled/
%  Commands available:
%    \screenshotslide{Title}{path}          — full-frame single image
%    \screenshotpair{Title}{L caption}{L path}{R caption}{R path}
%                                           — two images side by side
% ============================================================
\section{Optimization Action Analyzer}

\speakerslide{Khaled Mogahed}{%
  Optimization Action Analyzer \textbullet{}
  Configuration Tracking \textbullet{} Impact Quantification%
}

% ── 3.1  Overview (text slide)
\slide{Action Analyzer — Module Overview}

% ── 3.2  Dolt explanation (text slide)
\slide{Configuration Tracking with Dolt}

% ── 3.3  Action Analyzer UI screenshot
\screenshotslide{Action Analyzer — Interface}
               {screenshots/khaled/action_analyzer_ui.png}

% ── 3.4  Action Timeline screenshot
\screenshotslide{Action Timeline View}
               {screenshots/khaled/action_timeline.png}

% ── 3.5  Before / After overlay — side-by-side screenshots
\screenshotpair{Before / After KPI Impact Analysis}
               {Before Configuration Change}
               {screenshots/khaled/before_change.png}
               {After Configuration Change}
               {screenshots/khaled/after_change.png}

% ── 3.6  Delta bar chart screenshot
\screenshotslide{KPI Delta Bar Chart}
               {screenshots/khaled/delta_bar_chart.png}
